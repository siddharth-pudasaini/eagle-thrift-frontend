import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2FA450",
    },
    secondary: {
      main: "#E87A3C",
    },
    error: {
      main: "#DD4343",
    },
  },
});

export default theme;
