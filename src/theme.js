import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#2c7d41",
    },
    secondary: {
      main: "#d4540d",
    },
    error: {
      main: "#DD4343",
    },
  },
});

export default theme;
